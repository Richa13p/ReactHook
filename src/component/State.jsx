//state hooks
//useState
//useReducer
// State lets a component 
// “remember” information like user 
// input. For example, a form component can use state to store the input value, 
// while an image gallery component can use state to 
// store the selected image index.
import React, { useState } from 'react'
const [index , setIndex]= useState(0);
const State = () => {
  return (
    <div></div>
  )
}

export default State